# Python environment for CPSC 483 supervised learning (EDA → train → evaluate).
# Usage: cd ml && python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
jupyter>=1.0
ipykernel>=6.29
kaggle>=1.6
fastapi>=0.115
uvicorn[standard]>=0.32
